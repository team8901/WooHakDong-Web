const TossPayIcon = ({ className }: { className?: string }) => {
  return (
    <img
      alt="토스페이로 결제하기"
      src="/images/tossPay.svg"
      className={className}
    />
  );
};

export default TossPayIcon;
